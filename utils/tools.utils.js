/**
 * toBoolean
 *
 * @description This function convert string to boolean.
 * @param {String} string
 * @returns {Boolean}
 */
export const toBoolean = (string) => {
    if (string === "true") return true;
    if (string === "false") return false;
};
